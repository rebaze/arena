dependencies {
    api project(":arena-api")
    compile('io.kubernetes:client-java:1.0.0-beta2')
}

jar {
    manifest {
        attributes(
                'Automatic-Module-Name': 'org.cloudarena.kubernetes'
        )
    }
}
