dependencies {
    api project(":arena-api")
    implementation libraries.docker_client
}

jar {
    manifest {
        attributes(
                'Automatic-Module-Name': 'org.cloudarena.docker'
        )
    }
}
